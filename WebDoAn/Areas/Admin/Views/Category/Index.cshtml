@model IEnumerable<WebDoAn.Models.Category>

@{
    ViewData["Title"] = "Quản lý loại hàng hóa";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<link href="~/Adminassets/vendors/datatables/dataTables.bootstrap.min.css" rel="stylesheet">

<div class="page-header">
    <div class="header-sub-title">
        <nav class="breadcrumb breadcrumb-dash">
            <a asp-area="Admin" asp-controller="Home" asp-action="Index" class="breadcrumb-item"><i class="anticon anticon-home m-r-5"></i>Home</a>
            <span class="breadcrumb-item active">Danh sách loại hàng hóa</span>
        </nav>
    </div>
</div>
<hr />

<div class="card">
    <div class="card-header d-flex align-items-center">
        <h4 class="card-title">Danh sách loại hàng hóa</h4>
        <div>
            <a asp-area="Admin" asp-controller="Category" asp-action="Create" class="btn btn-primary">Tạo mới</a>
        </div>
    </div>
    <div class="card-body">
        <div class="block_search">
            <input class="input_search" id="SearchInput" placeholder="Nhập tên loại hàng hóa"/>
            <button id="Search" class="btn_search ml-2">Tìm kiếm</button>
        </div>
        <div class="table-responsive">
            <table id="data-table" class="table table-category w-100">
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>
                            Thời gian tạo
                        </th>
                        <th>
                            Tên loại
                        </th>
                        <th>
                            Trạng thái
                        </th>
                        <th class="text-center">#</th>
                    </tr>
                </thead>
                <tbody>
                  
                    @* @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                               
                            </td>
                            <td>
                                <p>@item.CreateTime.Date.ToShortDateString()</p>
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @if(item.Status == true)
                                {
                                    <span class="badge badge-success">Đang hoạt động</span>
                                }
                                else
                                {
                                    <span class="badge badge-danger">Chưa áp dụng</span>
                                }
                            </td>
                            <td class="text-center">
                                <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-info m-r-5">Xem chi tiết</a>
                                <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-warning m-r-5">Sửa</a>
                                <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger m-r-5">Xóa</a>
                            </td>
                        </tr>
                    } *@
                </tbody>
            </table>
        </div>
    </div>
</div>


<script src="~/lib/jquery/dist/jquery.min.js"></script>
<script src="~/lib/moment/moment.min.js"></script>
<script src="~/lib/moment/locale/vi.js"></script>

<script src="~/Adminassets/vendors/datatables/jquery.dataTables.min.js"></script>
<script src="~/Adminassets/vendors/datatables/dataTables.bootstrap.min.js"></script>
<script src="~/customJs/Admin/Category/Index.js"></script>